P1的目的，是修掉已经能用明确反例证明的问题。这一阶段最适合你开始读代码并参与修改。
主要工作集中在[supervisor.py (line 488)](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_vendor_omni/supervisor.py:488)。不必一开始拆整个文件，按以下六个小改动分别完成。
工作项	修改函数或位置	应如何改	如何验证
位置到了仍平移	vector_command()、tick_connector()	增加位置收敛状态；进入内阈值后平移目标归零，漂出外阈值才恢复调整；航向独立收敛	距离2 cm、角度未到时，平移减速至零；边界抖动不反复启停
直线超调误判	run_formation_line()约1717行	用径向距离判断位置到达；保留有符号距离判断是否超调；超调不能直接算成功	终点前、终点后、斜向偏离，分别检查到达/超调/未到达
最低速度切换	synchronized_forward_speeds()约74行	区分巡航同步和末端减速；末端不强制使用巡航最低速度	快慢车和毫米级测量扰动下，目标速度不频繁在零与最低速度间切换
连接器等待后跳目标	execute_gated_transition()约3383行	固定待批准目标及其版本；候选目标可以更新，但不能自动成为执行目标；升级目标须完整重新批准	重现节点16受阻、候选变19，确认没有未经重新批准的19目标
恢复阻塞无关车辆	transition_dispatch_allowed()及其调用处	输入冲突集合，而非只输入单一恢复车；检查实际占用、已承诺路径和停车范围	三车交叉场景：A恢复、B与A冲突、C无关；B等待、C继续
控制切换竞态	transition_claim_motion_owner()、ticket检查、目标取消/重发分支	取消、停车、所有权变更、新目标接收顺序明确；旧ticket不能重新激活	延迟回调、重复取消、旧成功结果、传感器恢复重发交错


例如，短段控制现在近似于：
位置和角度都到 → 停
否则 → 同时计算平移和旋转
建议变成：
位置未收敛 → 调整位置
位置已收敛 → 平移减速至零，继续调角度
位置、角度均收敛且停稳 → 完成
这里需要一个持续存在的状态，不能只在每一帧写if distance < threshold。否则定位在阈值附近变化，仍会反复切换。内外阈值可先沿用已有2.5/3.5 cm设计，再由P2判断是否适合实车。
超调后的处理也不能直接补一个“倒车回来”：当前位置到终点的修正路径必须位于批准范围内，并通过障碍和队友检查，否则输出“需要重新规划”。
现有测试入口是：
- [test_transition_connectors.py](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/test/test_transition_connectors.py)
- [test_transition_gates.py](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/test/test_transition_gates.py)
- [test_six_robot_vendor_omni_contract.py](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/test/test_six_robot_vendor_omni_contract.py)
注意，当前部分测试正好固化了旧行为：一个测试明确断言“有恢复车时另一车不能派发”，另一个只检查连接器超时后进入TEB。修改时需要用新的行为要求替换这些断言，不能为了让旧测试通过保留问题。
P1通过条件是：反例消失、固定任务仿真不新增碰撞或错误完成、单车与三车低速运行符合预期。完全排除过期速度执行，是P3的验收责任，P1不能提前承诺。
P2的目的，是测清每辆车实际能做到什么，并让仿真反映这些能力。它决定后面算法有没有可靠的执行基础。
先测量，再选择是否修改里程计。当前关键链路是：
[odom_publisher.py (line 169)](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_driver/jetauto_controller/scripts/odom_publisher.py:169)接收命令并积分；[ekf.launch (line 19)](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_slam/launch/include/ekf.launch:19)融合命令里程计与IMU，输出odom。
因此：
cmd=0、odom速度=0能够证明软件估计停了，但还不足以证明实车已经物理停稳。

具体要交付两样东西。
第一样是每车能力表：
输入：命令、独立实测位置/速度、AMCL、odom、IMU、时间戳
输出：启动死区、延迟、速度偏差、停车距离、
      定位偏差/抖动、适用速度与不可靠工况
可把[robot9_forward_probe.py](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_admin/robot9_forward_probe.py)改造成名单可选、方向可选的有界测试工具。它现在仅支持Robot9前进/停止，不能直接承担六方向标定。
测试先选一辆做完整流程，再推广十辆。计划中的“10车×6方向×3次=180片段”可作为首轮筛查；统计单位是一次完整短测试，不是bag中的每一帧。电量、地面、负载和外参需随记录保留。
里程计按证据分支：
- 有真实编码器反馈：在控制板节点/SDK接入轮速，再评估EKF融合。
- 没有反馈：评估激光局部里程计。仓库已有RF2O，但当前底层launch里的激光里程计接入被注释；需要单独试验。
- 仅做命令比例标定：可以改善固定偏差，但仍是预测，不能称作实测里程计。
第二样是可切换的非理想仿真。
主要修改：
- [sim_adapter.py (line 146)](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_vendor_omni/sim_adapter.py:146)
- [fleet_profile.py的仿真装配 (line 228)](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_vendor_omni/fleet_profile.py:228)
- [robot_navigation.launch](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/launch/fleet_vendor_omni/include/robot_navigation.launch)
需要拆开三个状态：
真实运动状态 → 生成雷达/IMU；仅评估器可直接读取
里程计状态   → 根据命令模型或运动反馈生成odom
AMCL估计     → 真正运行AMCL，根据雷达、地图、TF计算
当前三者高度一致，而且仿真直接发布AMCL、关闭真实AMCL。切换后必须让真实AMCL独占map→odom，不能同时保留适配器的恒等变换。
还有两处仿真缺口：
- 当前雷达只射线检测静态地图，没有把队友和移动障碍加入扫描。
- 仿真停车超时不能默认当作实车已有能力，应设为明确的能力配置。
验收应比较同一场景、同一车辆、同一扰动种子下的前后表现，分别报告真实误差和估计误差。单帧AMCL的“样本散布为零”是计算必然，不能作为定位稳定的证据。
P3的目的，是让每辆车只有一套有效执行状态，使TEB、短段调整和直线编队接受相同约束。它是整份计划中工程改造最大的一阶段。
当前存在两条直接速度链：
move_base/TEB ──────→ 底盘cmd_vel
supervisor直线/短段 ─→ 同一个底盘cmd_vel
owner_epoch在supervisor内部，但底盘收到的是普通Twist，没有目标版本或控制权版本。底层还订阅另一条cmd_vel输入，部署时也必须核对是否存在绕过统一出口的来源。
建议先完成一个单车纵向原型，接口定成下面的形式；这是拟新增接口，不是当前已有能力：
执行输入：
run_id、stage_id、route_version、owner_epoch
路径、坐标系、批准区域、已承诺前缀、有效期

执行输出：
已接受版本、执行模式、实际进度、定位质量
是否停稳、故障原因、是否请求重规划
按两步实现比较稳妥。
第一步，在当前包内抽出每车执行状态：
WAIT_APPROVAL → TRACK → ALIGN → VERIFY_STOP → DONE
                          ↘ HOLD / REQUEST_REPLAN
可新增robot_executor.py，承接当前tick_connector()、控制权管理和末端处理；supervisor保留阶段管理、跨车协调。先把TEB速度重映射为候选输入，统一执行出口负责有效性和模式判断。这个版本能统一来源，但还不能宣称对完整TEB轨迹完成发布前硬检查。
第二步，解决“同一次计算的轨迹和速度一起审核”。
建议技术验证采用受控导航插件：
- 全局规划接口返回协同批准路径，避免普通GlobalPlanner另选路线。
- 局部执行在返回速度前，检查候选轨迹、车体扫掠以及停车范围。
- 检查不通过就返回停止/重规划结果。
- 批准路径版本变化时，同步失效旧计算和旧回调。
需要修改robot_navigation.launch、fleet_profile.py、supervisor接口，并新增插件实现、插件描述、CMake/package依赖。厂商导航快照保留，通过本包覆盖接入。
这一步不能简单写“订阅local_plan后检查”：订阅到的路径未必与正在下发的速度属于同一次计算。官方Melodic接口显示TebLocalPlannerROS提供setPlan()和computeVelocityCommands()，内部planner_是私有成员，所以可能需要针对实际安装版本增加轨迹访问或受控封装。TEB官方接口
P3验证至少包括：
- 所有正常控制来源经过同一有效出口。
- 旧版本命令延迟到达时被拒绝。
- 暂停恢复、TEB失败、位置跳变时保留占用。
- 候选轨迹越出批准区域时，越界速度没有到底盘。
- 位置已到后末端不持续平移。
- 车端命令中断、连接恢复时行为正确。
计划把车端失联停车放在P4，我建议提前：P3应完成端到端命令有效期；更早的P1/P2实车测试前，也应先确认已有停车能力。
P4的目的，是让路径可以根据当前车辆和障碍状态更新，并让编队作出等待、绕行、收缩和重组决定。
当前[transition_gates.py (line 105)](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_vendor_omni/transition_gates.py:105)主要处理预先生成路径及其进度依赖；execute_gated_transition()运行时持有固定任务。仅循环调用原来的规划函数，不足以实现安全滚动规划。
建议新增三个职责明确的模块；下面名称是建议新增文件。
模块	输入	输出
dynamic_obstacles.py	新鲜scan、对应时刻TF、静态地图、队友状态	动态占用/障碍轨迹、置信度、有效时间
rolling_planner.py	当前位姿、目标、障碍、已承诺路径、计算预算	新路线、冲突依赖、批准区域、版本
formation_policy.py	当前队形、通道宽度、障碍状态、可行路线	等待、保持、收缩、分批、换队形或退出


现有代码的复用关系是：
- fleet_geometry.py继续提供地图和几何检查。
- transition_gates.py复用路径冲突和进度概念。
- formation_catalog.yaml提供候选队形。
- supervisor接收新计划，管理阶段和车辆确认。
- P3执行器执行批准结果，并反馈实际进度。
在线更新应有明确顺序：
取得同一时刻附近的车队状态
→ 固定当前占用和停车必需的承诺范围
→ 只规划可变的后续路径
→ 检查整个受影响冲突组
→ 相关执行器确认新版本
→ 同步生效
关键细节是：路径换了，原来的target_index、arc、cleared、gates不能原样继承。旧路线的“已经走过1.2 m”，不等于新路线也能释放1.2 m。必须按版本重新投影进度、重建相关依赖，并保留未确认车辆的旧占用。
雷达输入也要拆分。当前AMCL和两个costmap都使用去队友后的amcl_scan。主要修改：
- robot_navigation.launch：分别配置定位与导航输入。
- fleet_profile.py：传递这些接口。
- [amcl_teammate_scan_filter.py (line 99)](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/src/amcl_teammate_scan_filter.py:99)：按实际名单和扫描时刻处理队友。
- amcl_scan_filter.yaml：时间匹配和过滤参数。
导航保留队友占用后，原先的并行车道可能因costmap膨胀发生变化，必须回归固定任务，不能只改话题名称。
建议按四个独立功能交付：
1. 动态停车等待：障碍横穿时停住，清除后继续。
2. 静态在线绕行：原路线被堵，重新批准路线。
3. 动态绕行：考虑障碍的运动和不确定性。
4. 自主换队形：在已验证队形之间选择，并规划完整转换过程。
例如十车两列前进，障碍压缩了通道：
减速/停车 → 检查等待是否足够
→ 必要时生成单列方案
→ 规划十车转换路径
→ 通过窄处
→ 在可用区域恢复两列
当前生成器将目标航向固定为0，并限制直线编队沿地图+x方向。若未来要求沿弯曲通道保持朝向变化，需要扩展plan_profile.py及执行器的航向规则；旋转槽位模板并不会自动旋转车头。
P4首先做影子规划：只生成建议、不接管车辆。然后按单车、三车、十车验证。必须包含无解、规划超时、车辆失联、定位跳变、障碍跟踪丢失；这些情况下正确等待或停止就是合格结果。
P5的目的，是确认前面的改进能在十辆真实车辆上重复成立。它主要完善发布和验收流程。
复用当前运维入口：
- [ten_environment.bash](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_admin/ten_environment.bash)
- [check_ten_bottom.sh](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_admin/check_ten_bottom.sh)
- [start_ten_vendor_omni.sh](/Users/zhangmiaohan/Maomi/NKUworkshop/jetauto_swarm_wrc/jetauto_multi/scripts/fleet_admin/start_ten_vendor_omni.sh)
发布包关联P0的版本和参数记录，每次按：
版本与底层健康检查
→ 禁止运动时READY
→ 现场确认后低速运行
→ 完整五阶段
→ 独立位置验收
→ 保存成功和失败记录
静态基线与动态扩展分别统计。十次连续完整通过可以作为首轮工程门槛，但不能把十次调参后成功、或拼接不同运行的阶段成功，算作十次完整通过。
验收输出至少包含：各车真实终点误差与AMCL误差、最小净空、恢复次数、非必要停车次数、整次完成时间、失败原因和缺失数据。
你关心的“什么时候编译”，在这份计划中可以这样判断：
工作	编译与生效方式
P0记录脚本、P1现有Python逻辑	通常不编译；部署修改并重启相关节点
修改planning参数	重新生成mission；重启上层
P2新增Python仿真模型	通常不编译；新增节点安装声明时重新构建
修改IMU/EKF/底层配置	重启对应底层；按实际包同步部署
新增编码器C++接口或编译RF2O	编译相关包
P3导航插件、自定义消息/动作	必须编译并更新运行环境
P4纯Python规划策略	已有安装方式下通常不编译；新增接口定义则需要


编译应在实际ROS Melodic工作空间中进行。本仓库同时存在顶层活动包和src/下的旧副本，不能在任意目录编译后就认为生效；必须核对rospack find解析的包路径。当前文档中的Windows权威目录与这份Mac工作副本也需要在真正部署前统一。
对你最合适的学习和实施起点是：P0最小运行清单 → P1位置/航向分离 → P1超调 → P1连接器目标锁定 → 三车局部恢复 → P2实测。 每完成一项，你都能用一个具体例子讲清输入、输出、修改位置和验证结果，再进入P3、P4会容易得多。